apiVersion: apps/v1
kind: Deployment
metadata:
  name: keypouch-web
  namespace: keypouch
spec:
  replicas: 1
  selector:
    matchLabels: &id001
      app: keypouch-web
  template:
    metadata:
      labels: *id001
    spec:
      containers:
      - name: web
        image: keypouch-web:46770
        ports:
        - containerPort: 5173
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi
        livenessProbe:
          httpGet:
            path: /
            port: 5173
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /
            port: 5173
          initialDelaySeconds: 5
          periodSeconds: 5
        envFrom:
        - configMapRef:
            name: keypouch-configmap
        - secretRef:
            name: keypouch-secrets
