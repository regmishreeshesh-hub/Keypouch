# Database (use either DATABASE_URL or the DB_* variables)
DATABASE_URL=postgresql://admin:admin@localhost:5432/keypouch
DB_HOST=localhost
DB_PORT=5432
DB_NAME=keypouch
DB_USER=admin
DB_PASSWORD=admin

# JWT
JWT_SECRET=change-me

# Server
PORT=5001

# Health check
DB_READY_TIMEOUT_MS=1000
